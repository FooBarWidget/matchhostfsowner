[package]
name = "activatecontaineruid"
version = "0.9.0"
authors = ["Hongli Lai <honglilai@gmail.com>"]
edition = "2018"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
incremental = false
panic = "abort"

[dependencies]
yaml-rust = "^0.4"
chrono = "^0.4.0"
colored = "^1.6"
log = { version = "^0.4.6", features = ["std"] }
nix = "^0.14.0"
failure = "^0.1.5"
pwd = "^1.3.0"
libc = "^0.2"
shell-escape = "^0.1.4"
